# Componentes compartilhados

Use esta pasta para componentes reutilizados por mais de uma feature, mas que nao sejam primitives do Design System.

Componentes base como `Button`, `Card`, `Table`, `Dialog`, `Sheet`, `Tabs`, `Select` e `Toast` devem permanecer em `src/components/ui`.
